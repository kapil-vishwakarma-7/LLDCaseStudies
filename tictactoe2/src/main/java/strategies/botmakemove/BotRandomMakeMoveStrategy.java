package strategies.botmakemove;

import model.Board;
import model.Move;
import model.Player;

public class BotRandomMakeMoveStrategy implements BotMakeMoveStrategy{
    @Override
    public Move makeMove(Board board, Player player) {
        return null;
    }
}
